#!/usr/bin/env python3
"""takes_forever - infinite loop with a long sleep."""

import sys
import time


def main() -> int:
    while True:
        time.sleep(1000)
    print("This should never happen...", file=sys.stderr)
    return 0


if __name__ == "__main__":
    sys.exit(main())
