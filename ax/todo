# damit den ganzen stdout in omniopt in eine eigene datei pipen und die per oo share mitteilen

#bash_logname="test"

#exec 1> >(tee -ia $bash_logname)
#exec 2> >(tee -ia $bash_logname >& 2)

#ls

#echo "hallo" 2>&2
